services:
  soundwave:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/sc?ping=1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
